export default function Footer() {
  return (
    <footer className="mt-12 border-t border-zinc-800 py-6 text-center text-sm text-zinc-500">
      © {new Date().getFullYear()} BetComp — projeto demonstrativo (18+).
    </footer>
  );
}
